machine:
  timezone:
    Asia/Tokyo

  ruby:
    version:
      2.1.5

test:
  override:
    - echo 'No Tests. So skip it :)'

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy

notify:
  webhooks:
    - url: https://zapier.com/hooks/catch/3j7y00/
